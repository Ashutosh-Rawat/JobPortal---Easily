<!-- Job Add Modal -->
<div class="modal fade" id="addJobModal" tabindex="-1" aria-labelledby="addJobModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <!-- Modal Header -->
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="addJobModalLabel">Add Job</h5>
        <button type="button" class="btn-close text-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <!-- Modal Body -->
      <div class="modal-body">
        <form id="addJobForm" method="POST" action="/jobs/add">
          <!-- Category and Designation -->
          <div class="row mb-4">
            <div class="col-md-6">
              <label for="addJobCategory" class="form-label">Job Category</label>
              <select id="addJobCategory" name="category" class="form-select" required>
                <!-- Populated dynamically -->
              </select>
            </div>
            <div class="col-md-6">
              <label for="addJobDesignation" class="form-label">Job Designation</label>
              <select id="addJobDesignation" name="designation" class="form-select" required>
                <!-- Populated dynamically -->
              </select>
            </div>
          </div>
          <!-- Skills Required -->
          <div class="mb-4">
            <label class="form-label">Skills Required</label>
            <div id="addSkillsContainer" class="d-flex flex-wrap gap-2">
              <!-- Skills checkboxes generated dynamically -->
            </div>
          </div>
          <!-- Other Fields -->
          <div class="row mb-4">
            <div class="col-md-6">
              <label for="addLocation" class="form-label">Job Location</label>
              <input type="text" id="addLocation" name="location" class="form-control" required>
            </div>
            <div class="col-md-6">
              <label for="addCompanyName" class="form-label">Company Name</label>
              <input type="text" id="addCompanyName" name="companyName" class="form-control" required>
            </div>
          </div>
          <div class="row mb-4">
            <div class="col-md-6">
              <label for="addSalary" class="form-label">Salary</label>
              <input type="number" id="addSalary" name="salary" class="form-control" required>
            </div>
            <div class="col-md-6">
              <label for="addApplyBy" class="form-label">Apply By Date</label>
              <input type="date" id="addApplyBy" name="applyBy" class="form-control" required>
            </div>
          </div>
          <div class="row mb-4">
            <div class="col-md-6">
              <label for="addOpenings" class="form-label">Number of Openings</label>
              <input type="number" id="addOpenings" name="openings" class="form-control" min="1" required>
            </div>
          </div>
          <!-- Submit Button -->
          <button type="submit" class="btn btn-success w-100">Add Job</button>
        </form>
      </div>
    </div>
  </div>
</div>
